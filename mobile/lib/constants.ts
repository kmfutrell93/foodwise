export const FREE_PLAN_LIMIT = 3;
export const CHECKIN_TIMEOUT_MS = 20000;
/** Symptom insights Claude call — must not abort early or the profile write never lands. */
export const INSIGHT_TIMEOUT_MS = 60000;
export const GENERATION_TIMEOUT_MS = 60000;
export const GROCERY_TIMEOUT_MS = 60000;
